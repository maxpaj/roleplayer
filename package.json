{
  "name": "dnd",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-custom": "*",
    "prettier": "^3.1.1",
    "turbo": "^1.9.3"
  }
}
